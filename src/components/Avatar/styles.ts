import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  container: {
    width: 48,
    height: 48,
    marginRight: 22,
    borderRadius: 8,

    alignItems: 'center',
    justifyContent: 'center'
  },

  avatar: {
    width: 45,
    height: 45,
    borderRadius: 8
  }
})
