import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  container: {
    width: 64,
    height: 68,
    borderRadius: 8,
    marginRight: 20,

    alignItems: 'center',
    justifyContent: 'center'
  },

  icon: {
    width: 61,
    height: 65,
    borderRadius: 8
  }
})
