import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  container: {
    width: 64,
    height: 64,
    borderRadius: 8,
    marginRight: 20,

    alignItems: 'center',
    justifyContent: 'center'
  },

  icon: {
    width: 61,
    height: 61,
    borderRadius: 8
  }
})
